import React from 'react';
import { Container, Typography } from '@mui/material';

const ReviewForm = () => (
  <Container>
    <Typography variant="h5" gutterBottom>Submit a Review</Typography>
    {/* Review form will go here */}
  </Container>
);

export default ReviewForm;
